server:
  port: ${SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: prometheus,health
  metrics:
    export:
      prometheus:
        enabled: true
    use-global-registry: true

logging:
  level:
    root: ${LOG_LEVEL:INFO}
